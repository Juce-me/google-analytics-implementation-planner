interface:
  display_name: "Google Analytics Planner"
  short_description: "Codebase-grounded GA4 plans and MCP handoffs"
  default_prompt: "Use $google-analytics-implementation-planner to design a privacy-first GA4 instrumentation plan, setup runbook, durable analytics contract, future-feature analytics rule, and optional MCP execution handoff for this repo."

policy:
  allow_implicit_invocation: true
