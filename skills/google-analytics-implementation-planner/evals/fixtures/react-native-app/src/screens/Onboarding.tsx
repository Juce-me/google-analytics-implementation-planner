import { Button, View } from "react-native";

export function Onboarding() {
  return (
    <View>
      <Button title="Create workspace" onPress={() => {}} />
      <Button title="Invite teammate" onPress={() => {}} />
      <Button title="Finish" onPress={() => {}} />
    </View>
  );
}
