import { Button, View } from "react-native";

export function Subscribe() {
  return (
    <View>
      <Button title="Start trial" onPress={() => {}} />
      <Button title="Subscribe yearly" onPress={() => {}} />
    </View>
  );
}
